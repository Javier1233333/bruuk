{
  "name": "bruuk-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:web": "pnpm --filter @bruuk/web run dev",
    "build:web": "pnpm --filter @bruuk/web run build",
    "build:web:cap": "pnpm --filter @bruuk/web run build:cap",
    "test": "pnpm -r run test"
  }
}
